package ru.sbt.mipt.oop.sensor_commands;

public class ConsoleCommandSender implements CommandSender {
    @Override
    public void sendCommand(SensorCommand command) {
        // Pretend we are sending a command
    }
}
